defaults:
  - inference_base
  - _self_

run_name_: rebuttal_filtered_broteina_distillation
# ckpt_name: finetuned_designables_epoch=00000000_step=000000002000.ckpt
ckpt_name: filtered2_ss_contd2_chk_epoch=00000010_step=000000020800.ckpt

self_cond: True

sampling_caflow:
  sampling_mode: sc  # "vf" for ODE sampling, "sc" for SDE sampling
  sc_scale_noise: 0.45  # noise scale, used if sampling_mode == "sc"
